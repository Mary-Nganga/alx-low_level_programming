#include <stdio.h>

/**
  * main - Prints the program, and new line.
  * @argc: Arguement count/number
  * @argv: Argument vector , array of pointers to the arguments.
  *
  * return: 0
  */

int main(int __attribute__((__unused__)) argc, char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
